    
<div>
    <h2>Favorite an Author!</h2>
</div>

<div>
    <%= form_for @favorite do |f| %>
    <%# <%= f.label :user_id %>
    <%# <%= f.collection_select :user_id, @users, :id, :displayname %> 

    <%= f.label :author_id %>
    <%= f.collection_select :author_id, @authors, :id, :name %>

    <%= f.label :genre %>
    <%= f.text_field :genre %>

    <%= f.submit "Favorite this author!" %>
    <% end %>
</div>